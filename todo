
add Task Template: new Contact 2ter Input wenn neuer Contact


getInitialsForFullName - 2 mal contacts/signup

alphabetisch sortieren user, categorys - 2mal


bei task wird soritert und dann gepusht

bei contacts wird geladen und dann sortiert

addTask: drop down für gesamten input
button: task wurde erstellt und alle inhalte löschen
new Category bug

allgemeine error Funktion - id geben und d-none - gibt es schon?

Kontake müssen nicht bei Task erstellt werden

im ikognito modus kein local storage

nach Erstellung eines Tasks Weiterleitung an Board

Ladedinger anzeigen

Überlefung: Jeder Kontakt hat Id, wenn Kontakt gelöscht wird, dann task durchsuchen und id löschen


backend emai

btn animation -- task erstellt

form signin
email forgot password
animation login
button task erstellt


/**
 * Retrieves the texts of the subtasks.
 * @returns {string[]} An array of subtask texts.
 */
function getSubtasks() {
  const subtaskElements = document.querySelectorAll(".subtask");
  const subtaskTexts = [];
  subtaskElements.forEach((element) => {
    subtaskTexts.push(element.textContent.trim());
  });
  return subtaskTexts;
}

function toggleInviteContact() {
    toggleClass("newContact", "d-none");
    toggleClass("selectContact", "d-none");
  }
  
  function showSelectContacts() {
    toggleInviteContact();
    toggleClass("listContacts", "d-none");
  }

funktionen werden nicht genutzt?